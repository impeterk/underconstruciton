---
const { navActiveId } = Astro.props;
import NavLink from '../NavLink.astro';
---

<ul class="mt-10 mx-4 menu">
	<NavLink href="/" text="Home" id="home" navActiveId={navActiveId} />
	<NavLink href="/contact" text="Contact" id="contact" navActiveId={navActiveId} />
	<NavLink href="/now" text="Now" id="now" navActiveId={navActiveId} />
</ul>
